server {
    listen 80;
    server_name api_fittrack.jspoh.dev;

    location / {
        proxy_pass http://127.0.0.1:1363;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}